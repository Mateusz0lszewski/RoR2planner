"""from RoRplanner.models import Item
import pytest

# Create your tests here.
@pytest.mark.django_db
def test_item_model(item):
    assert len(Item.objects.all()) == 1
    assert Item.objects.get(name='Przedmiot1') == item
"""